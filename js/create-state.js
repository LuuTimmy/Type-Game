class CreateState
{
    constructor(name, staggerFrames, frames, xPos, yPos, isLoop) {
        this.name = name;
        this.staggerFrames = staggerFrames;
        this.frames = frames;
        this.posX = xPos;
        this.posY = yPos;
        this.isLoop = isLoop;
    }
}